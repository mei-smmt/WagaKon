<%= form_with(url: relationships_path, method: :delete) do |f| %>
  <%= hidden_field_tag :user_id, friend.id %>
  <%= f.submit delete_message, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-sm button p-0' %>
<% end %>
