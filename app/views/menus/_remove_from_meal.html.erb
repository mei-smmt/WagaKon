<%= form_with(model: current_user.meals.day(day).menus.find_by(recipe_id: recipe.id), method: :delete, local: true) do |f| %>
  <%= hidden_field_tag :recipe_id, recipe.id %>
  <%= hidden_field_tag :day, day %>
  <%= f.submit 'x', class: 'btn', id: 'remove-menu-btn' %>
<% end %>
